{
  "name": "personal-link-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply link_hub --local",
    "db:migrate:remote": "wrangler d1 migrations apply link_hub --remote",
    "hash:password": "node scripts/hash-password.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260511.1",
    "playwright-core": "^1.60.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.90.1"
  }
}
